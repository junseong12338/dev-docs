# 10. 빠싺 최종시험 - Black Hole

---

!!! danger "최종시험 안내"
    "There are no two words in the English language
    more harmful than 'good job'."

    01~09 전 범위. 30문제. 100점 만점.
    여기서 80점 못 넘기면 처음부터 다시 읽어.

## 시험 규칙

| 항목 | 규칙 |
|------|------|
| **합격 기준** | 80점 이상 |
| **시간 제한** | 60분 |
| **오픈북** | 금지. 머리에 없으면 모르는 거야. |
| **검색** | 금지. 구글 켜는 순간 불합격. |
| **부분 점수** | 서술형만 부분 점수 있음 |
| **총점** | 100점 |

## 배점 구성

| 유형 | 문항 수 | 문항당 점수 | 합계 |
|------|---------|-------------|------|
| 객관식 (4지선다) | 10문제 | 2점 | 20점 |
| 단답형 | 8문제 | 3점 | 24점 |
| 코드 작성/분석 | 7문제 | 5점 | 35점 |
| 서술형 | 5문제 | ~4-5점 | 21점 |
| **합계** | **30문제** | | **100점** |

---

# Part 1. 객관식 (10문제 x 2점 = 20점)

---

### Q1. [01_메모리란_무엇인가] (2점)

CPU가 데이터를 처리할 때, 왜 HDD/SSD에서 직접 읽지 않고 반드시 RAM을 거치는가? 가장 정확한 이유를 고르시오.

(A) HDD/SSD는 읽기 전용이라 쓰기가 안 되기 때문에
(B) CPU의 처리 속도와 HDD/SSD의 접근 속도 차이가 너무 커서, 중간에 빠른 매체가 필요하기 때문에
(C) 운영체제가 HDD/SSD 직접 접근을 법적으로 금지하기 때문에
(D) RAM이 HDD/SSD보다 저장 용량이 크기 때문에

---

### Q2. [02_가상_메모리와_페이징] (2점)

가상 메모리(Virtual Memory)를 사용하는 가장 핵심적인 이유는?

(A) 프로세스마다 독립된 주소 공간을 제공하여 메모리 보호와 격리를 보장하기 위해
(B) RAM을 더 빠르게 만들기 위해
(C) CPU 캐시 적중률을 높이기 위해
(D) 디스크 I/O 속도를 향상시키기 위해

---

### Q3. [03_프로세스_메모리_구조] (2점)

Stack과 Heap을 분리한 근본적인 이유는?

(A) Stack이 Heap보다 용량이 커서
(B) Stack은 컴파일 타임에, Heap은 런타임에 크기가 결정되므로 할당/해제 전략이 근본적으로 다르기 때문에
(C) 운영체제가 Stack만 관리하고 Heap은 하드웨어가 관리하기 때문에
(D) Stack은 읽기만, Heap은 쓰기만 가능하기 때문에

---

### Q4. [04_JVM_메모리_구조] (2점)

JVM이 Heap을 Young Generation과 Old Generation으로 나눈 이유의 근거가 되는 가설은?

(A) 강한 참조 가설 (Strong Reference Hypothesis)
(B) 약한 세대 가설 (Weak Generational Hypothesis)
(C) 메모리 지역성 가설 (Memory Locality Hypothesis)
(D) 객체 불변 가설 (Object Immutability Hypothesis)

---

### Q5. [05_가비지_컬렉션_기초] (2점)

GC Root가 될 수 **없는** 것은? (함정 문제)

(A) 현재 실행 중인 스레드의 로컬 변수
(B) static 변수
(C) Old Generation에 있는 일반 객체
(D) JNI로 참조되는 네이티브 객체

---

### Q6. [05_가비지_컬렉션_기초] (2점)

Stop-The-World(STW)에 대한 설명 중 틀린 것은?

(A) GC가 동작하는 동안 애플리케이션 스레드가 멈추는 현상이다
(B) Minor GC에서는 STW가 발생하지 않는다
(C) STW 시간이 길어지면 서비스 응답 지연이 발생한다
(D) G1 GC는 STW 시간을 예측 가능하게 만드는 것이 목표다

---

### Q7. [06_메모리_누수_패턴] (2점)

Java에서 "메모리 누수"의 정확한 정의는?

(A) 객체를 너무 많이 생성하는 것
(B) GC가 회수할 수 없는 참조가 남아 있어서, 사용하지 않는 객체가 계속 메모리를 점유하는 것
(C) Heap 사이즈를 너무 크게 설정한 것
(D) OutOfMemoryError가 발생하는 모든 상황

---

### Q8. [07_리눅스_메모리_관리] (2점)

`free -h` 출력에서 `buff/cache` 값이 높을 때, 이것이 메모리 부족 상황인가?

(A) 무조건 메모리 부족이다. 즉시 프로세스를 종료해야 한다.
(B) buff/cache는 필요 시 OS가 자동으로 회수하므로, 그 자체로는 메모리 부족이 아니다.
(C) buff/cache는 절대 회수되지 않는 영역이다.
(D) buff/cache는 Swap과 동일한 영역이다.

---

### Q9. [08_JVM_튜닝과_진단도구] (2점)

`-Xms`와 `-Xmx`를 동일한 값으로 설정하는 이유는?

(A) JVM 시작 속도를 높이기 위해
(B) 힙 크기 동적 조절 과정에서 발생하는 오버헤드와 Full GC를 방지하기 위해
(C) Old Generation 크기를 고정하기 위해
(D) Metaspace 크기를 제한하기 위해

---

### Q10. [09_실전_메모리_장애대응] (2점)

운영 중 OOM(OutOfMemoryError)이 발생했을 때, 가장 먼저 해야 할 행동은?

(A) 즉시 코드를 수정하여 배포한다
(B) JVM 힙 사이즈를 2배로 늘려서 재시작한다
(C) 힙덤프를 확보하고, 서비스 영향도를 파악한 뒤 임시 조치(재시작 등)를 취한다
(D) GC 알고리즘을 변경하여 재시작한다

---

# Part 2. 단답형 (8문제 x 3점 = 24점)

---

### Q11. [01_메모리란_무엇인가] (3점)

메모리 계층 구조를 **빠른 순서대로** 4단계로 나열하시오. (레지스터 포함)

---

### Q12. [02_가상_메모리와_페이징] (3점)

Page Fault가 발생하는 상황을 한 문장으로 정의하시오.

---

### Q13. [03_프로세스_메모리_구조] (3점)

Stack Overflow가 발생하는 가장 전형적인 원인 2가지를 쓰시오.

---

### Q14. [04_JVM_메모리_구조] (3점)

JVM 메모리 구조에서 Java 8 이후 PermGen이 사라지고 대체된 영역의 이름과, 그 영역이 존재하는 위치(Heap 내부/외부)를 쓰시오.

---

### Q15. [06_메모리_누수_패턴] (3점)

Java에서 static 컬렉션(예: `static List`)이 메모리 누수를 일으키는 이유를 한 문장으로 쓰시오.

---

### Q16. [07_리눅스_메모리_관리] (3점)

리눅스 OOM Killer가 프로세스를 종료할 때 사용하는 점수의 이름은 무엇이며, 이 점수가 높을수록 어떻게 되는가?

---

### Q17. [08_JVM_튜닝과_진단도구] (3점)

운영 중인 JVM에서 힙덤프를 뜨기 위해 사용하는 `jcmd` 명령어의 형태를 쓰시오. (PID는 12345로 가정)

---

### Q18. [09_실전_메모리_장애대응] (3점)

OOM 발생 시 자동으로 힙덤프를 생성하게 하는 JVM 옵션 2가지를 쓰시오.

---

# Part 3. 코드 작성/분석 (7문제 x 5점 = 35점)

---

### Q19. [06_메모리_누수_패턴] (5점)

아래 코드에서 메모리 누수가 발생하는 부분을 찾고, **왜** 누수인지 설명하시오.

```java
public class EventManager {
    private static final List<EventListener> listeners = new ArrayList<>();

    public void addListener(EventListener listener) {
        listeners.add(listener);
    }

    // removeListener 메서드 없음
}

public class UserSession {
    public void init() {
        EventManager manager = new EventManager();
        manager.addListener(new EventListener() {
            @Override
            public void onEvent(Event e) {
                // 세션별 이벤트 처리
            }
        });
    }
    // UserSession 종료 시 listener 제거 로직 없음
}
```

(1) 누수 지점을 지목하시오.
(2) 왜 GC가 이 객체를 회수하지 못하는지 설명하시오.
(3) 해결 방법을 쓰시오.

---

### Q20. [05_가비지_컬렉션_기초] (5점)

아래 코드 실행 후, GC가 회수할 수 있는 객체와 회수할 수 없는 객체를 구분하시오.

```java
public void process() {
    Object a = new Object();  // 객체 A
    Object b = new Object();  // 객체 B
    Object c = new Object();  // 객체 C

    a = b;      // (1)
    b = null;   // (2)
    c = a;      // (3)
}
// 메서드 종료 직전 시점 기준
```

(1) 메서드 종료 직전, 각 객체(A, B, C)의 참조 상태를 설명하시오.
(2) GC가 회수 가능한 객체를 모두 고르시오.
(3) 이유를 설명하시오.

---

### Q21. [04_JVM_메모리_구조] (5점)

아래 코드에서 각 요소가 JVM 메모리의 어디에 저장되는지 매핑하시오.

```java
public class Example {
    private static final String GREETING = "Hello";   // (1)
    private int count;                                 // (2)

    public void doSomething() {
        int localVar = 10;                             // (3)
        String name = new String("World");             // (4)
        List<Integer> list = new ArrayList<>();        // (5)
    }
}
```

각 번호에 대해 [Stack / Heap / Metaspace] 중 어디에 저장되는지 쓰시오. 참조 변수와 실제 객체를 구분해서 답하시오.

---

### Q22. [07_리눅스_메모리_관리] (5점)

아래 `free -h` 출력을 해석하시오.

```
              total        used        free      shared  buff/cache   available
Mem:           15Gi       13Gi       256Mi       128Mi        2Gi        1.8Gi
Swap:         4.0Gi       3.5Gi       512Mi
```

(1) 실제로 애플리케이션이 사용 가능한 메모리는 얼마인가?
(2) 이 서버는 메모리 부족 상태인가? 그렇게 판단한 근거를 2가지 이상 쓰시오.
(3) Swap 사용량이 높다는 것은 무엇을 의미하는가?

---

### Q23. [06_메모리_누수_패턴] (5점)

아래 코드에서 잠재적 메모리 누수 패턴을 찾고 수정하시오.

```java
public class ResourceHandler {
    public String readFile(String path) throws IOException {
        BufferedReader reader = new BufferedReader(new FileReader(path));
        StringBuilder content = new StringBuilder();
        String line;
        while ((line = reader.readLine()) != null) {
            content.append(line);
        }
        return content.toString();
    }
}
```

(1) 누수 패턴의 이름을 쓰시오.
(2) 어떤 상황에서 누수가 발생하는지 설명하시오.
(3) 수정된 코드를 작성하시오.

---

### Q24. [08_JVM_튜닝과_진단도구] (5점)

다음 JVM 옵션을 해석하고, 각 옵션이 어떤 상황을 해결하기 위한 것인지 설명하시오.

```
-Xms4g -Xmx4g -XX:+UseG1GC -XX:MaxGCPauseMillis=200
-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/var/log/dumps/
```

(1) 각 옵션의 의미를 쓰시오. (6개 모두)
(2) `-Xms`와 `-Xmx`를 같은 값으로 설정한 이유를 쓰시오.
(3) G1 GC를 선택한 이유로 적절한 상황을 1가지 쓰시오.

---

### Q25. [03_프로세스_메모리_구조] (5점)

아래 C 코드의 각 변수가 메모리의 어느 영역(Code/Data/BSS/Stack/Heap)에 저장되는지 쓰시오.

```c
#include <stdlib.h>

int globalVar = 100;           // (1)
int uninitGlobal;              // (2)
const char* STR = "constant";  // (3) 문자열 리터럴 "constant"이 저장되는 위치

void func() {
    int localVar = 5;          // (4)
    int* heapVar = (int*)malloc(sizeof(int));  // (5) malloc으로 할당된 메모리
    static int staticVar = 0;  // (6)
}
```

---

# Part 4. 서술형 (5문제 = 21점)

---

### Q26. [02_가상_메모리와_페이징] (4점)

Thrashing(스래싱)이 발생하는 원인과 증상을 설명하고, 해결 방법 2가지를 서술하시오.

---

### Q27. [04_JVM_메모리_구조 + 05_가비지_컬렉션_기초] (5점)

객체 하나가 `new`로 생성된 시점부터 GC에 의해 회수되기까지의 전체 여정을 서술하시오. 아래 키워드를 반드시 포함할 것:

> Eden, Survivor, Old Generation, Minor GC, Major GC, Age, Promotion

---

### Q28. [05_가비지_컬렉션_기초] (4점)

Serial GC, Parallel GC, G1 GC 세 가지를 비교하여 각각 어떤 환경에서 선택해야 하는지 서술하시오. 단순히 "빠르다/느리다"가 아니라 **왜** 그 환경에서 적합한지 근거를 포함할 것.

---

### Q29. [09_실전_메모리_장애대응] (4점)

운영 서버에서 "java.lang.OutOfMemoryError: Java heap space"가 발생했다. 원인을 진단하기 위한 순서를 5단계로 서술하시오. 각 단계에서 사용하는 도구/명령어를 함께 쓸 것.

---

### Q30. [01~09 통합] (4점)

"메모리"라는 키워드가 나왔을 때, OS 레벨부터 JVM 레벨까지 전체 흐름을 3문장 이내로 요약하시오. 아래 계층을 모두 포함할 것:

> 물리 메모리 -> 가상 메모리 -> 프로세스 메모리 구조 -> JVM 메모리 -> GC

---

!!! quote "시험 끝"
    다 풀었으면 정답지(11_빠싺_최종시험_정답.md) 열어서 채점해.
    80점 못 넘겼으면 틀린 챕터 다시 읽고 와.
    80점 넘겼으면? 다음 단계로.

    "Not quite my tempo? 다시."
