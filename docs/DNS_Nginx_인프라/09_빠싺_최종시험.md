# 09. 빠싺 최종시험 - DNS / Nginx 인프라 완전정복

!!! danger "난이도: Black Hole"
    01~08장 전 범위. 30문제, 100점 만점.
    **80점 이상이면 합격.** 미만이면 처음부터 다시.

!!! warning "규칙"
    - 답을 보지 말고 **직접** 적어볼 것
    - "대충 아는 것 같은데"는 모르는 거야
    - 모든 답에 **"왜?"**까지 적을 수 있어야 진짜 아는 거야
    - 10장에 정답 + 상세 해설 있음

---

## Section A: IP와 포트 (20점)

!!! question "Q1. (3점) 우리 WAS 서버의 공인 IP와 내부 IP를 적고, 각각 어디서 사용하는지 설명하라."

!!! question "Q2. (3점) 포트의 본질은 뭐냐? 포트가 없으면 어떤 문제가 생기는지 설명하라."

!!! question "Q3. (4점) 아래 명령어의 차이를 설명하라."
    ```bash
    hostname -I
    curl ifconfig.me
    ```

!!! question "Q4. (4점) `sudo lsof -i :8082` 실행 결과가 없으면 무슨 뜻이야? 이 상황에서 어떻게 해야 해?"

!!! question "Q5. (3점) HTTPS의 기본 포트는? HTTP의 기본 포트는? 브라우저에서 `https://nexclass.knu10.cc`를 치면 포트가 안 보이는 이유는?"

!!! question "Q6. (3점) Well-Known 포트 5개를 쓰고, 각각 뭐에 쓰이는지 적어라."

---

## Section B: DNS와 도메인 (20점)

!!! question "Q7. (4점) DNS의 본질을 한 문장으로 설명하고, `nslookup nexclass.knu10.cc`의 결과가 뭐가 나오는지 적어라."

!!! question "Q8. (4점) A 레코드와 CNAME 레코드의 차이를 설명하고, www.knu10.cc가 CNAME인 이유를 설명하라."

!!! question "Q9. (4점) TTL이 3600초인 상태에서 A 레코드를 잘못 설정했다. 최악의 시나리오를 설명하라."

!!! question "Q10. (4점) 우리 Cloudflare DNS에 Sectigo 관련 CNAME이 2개 있다. 이게 뭐고 왜 남겨두는 거야?"

!!! question "Q11. (4점) nexclass.knu10.cc의 DNS 조회 과정을 순서대로 설명하라. (브라우저 캐시부터)"

---

## Section C: HTTP/HTTPS와 SSL (20점)

!!! question "Q12. (3점) HTTP와 HTTPS의 핵심 차이 3가지를 적어라."

!!! question "Q13. (4점) Zoom이 Webhook URL에 HTTPS만 허용하는 이유를 설명하라."

!!! question "Q14. (3점) 아래 Nginx 설정이 하는 일을 설명하라."
    ```nginx
    return 301 https://$host$request_uri;
    ```

!!! question "Q15. (4점) SSL 인증서 파일 2종류(.crt.pem, .key.pem)의 차이와, .key.pem이 유출되면 어떻게 되는지 설명하라."

!!! question "Q16. (3점) TLS 핸드셰이크 과정을 간략히 설명하라."

!!! question "Q17. (3점) 우리 인증서 하나로 nexclass.knu10.cc, lms.knu10.cc, hub.knu10.cc 전부 커버 가능한 이유는?"

---

## Section D: Nginx 리버스 프록시 (20점)

!!! question "Q18. (4점) 리버스 프록시의 본질을 설명하고, 포워드 프록시와의 차이를 적어라."

!!! question "Q19. (4점) 아래 설정을 한 줄씩 설명하라."
    ```nginx
    proxy_pass http://10.7.1.52:8082;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header HOST $http_host;
    ```

!!! question "Q20. (4점) 같은 IP(49.247.41.134)로 4개 도메인(www, lms, nexclass, hub)이 들어오는데, Nginx가 어떻게 다른 백엔드로 보내?"

!!! question "Q21. (4점) `nginx -t`를 안 하고 `nginx -s reload`를 했는데 설정에 오타가 있었다. 벌어지는 일과 올바른 순서를 적어라."

!!! question "Q22. (4점) SSL 종료(SSL Termination)가 뭐고, 왜 Nginx에서 하는 거야?"

---

## Section E: WEB/WAS 아키텍처와 실전 (20점)

!!! question "Q23. (4점) WEB/WAS를 분리하는 이유 4가지를 적어라."

!!! question "Q24. (4점) NexClass가 장애로 죽었다. LMS와 HUB에 영향이 가는가? 이유와 함께 설명하라."

!!! question "Q25. (4점) Cloudflare에서 nexclass A 레코드를 추가할 때, Proxied가 아닌 DNS Only로 설정한 이유는?"

!!! question "Q26. (4점) 아래 URL로 요청이 들어왔을 때, DNS → WEB → WAS까지의 전체 흐름을 설명하라."
    ```
    https://nexclass.knu10.cc/webhook/zoom
    ```

!!! question "Q27. (4점) 오늘 인프라 설정에서 실제로 한 작업은 딱 2가지였다. 뭐랑 뭐야? 그리고 나머지(SSL 등)는 왜 안 해도 됐어?"

---

## 보너스 문제 (10점)

!!! question "Q28. (3점) 내부 통신(Nginx → NexClass)은 HTTP인데 보안상 문제 없는 이유는?"

!!! question "Q29. (3점) proxy_connect_timeout 1800이 의미하는 것과, 왜 기본값(60초)보다 크게 설정했는지 설명하라."

!!! question "Q30. (4점) 만약 WEB/WAS 분리 없이 서버 1대에 Nginx + NexClass + LMS + HUB + DB를 전부 올렸다면, 어떤 문제들이 생길 수 있는지 3가지 이상 적어라."

---

!!! danger "채점 기준"
    | 점수 | 등급 | 판정 |
    |:----:|:----:|------|
    | 90~110 | 빠싺 | "이제 좀 개발자 같네" |
    | 80~89 | 합격 | "통과. 근데 빈틈 있어" |
    | 70~79 | 재시험 | "부족해. 해당 장 다시 읽어" |
    | ~69 | 처음부터 | "01장부터 다시. 건너뛴 거 있지?" |

**10장에서 정답 확인.**
