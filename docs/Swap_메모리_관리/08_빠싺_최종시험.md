# 8. 빠싺 최종시험 - Black Hole

---

!!! danger "시험 규칙"
    01~07 전 범위. 30문제. 100점 만점.
    여기서 80점 못 넘기면 처음부터 다시 읽어.

    **"There are no two words in the English language more harmful than 'good job'."**

| 항목 | 규칙 |
|------|------|
| **합격 기준** | 80점 이상 |
| **시간 제한** | 60분 |
| **오픈북** | 금지. 머리에 없으면 모르는 거야. |
| **검색** | 금지. 구글 켜는 순간 불합격. |
| **부분 점수** | 서술형만 부분 점수 있음 |
| **총점** | 100점 |

---

## 문제 구성

| 유형 | 문항 번호 | 문항당 점수 | 합계 |
|------|-----------|-------------|------|
| 객관식 (4지선다) | Q1~Q10 | 2점 | 20점 |
| 단답형 | Q11~Q18 | 3점 | 24점 |
| 코드 작성/분석 | Q19~Q25 | 5점 | 35점 |
| 서술형 | Q26~Q30 | 4~5점 | 21점 |

---

## Part 1. 객관식 (Q1~Q10, 각 2점)

---

### Q1. [Ch01] (2점)

`free -h`에서 Mem 줄의 total이 16GB, used가 6GB, buff/cache가 8GB다. free는 얼마야?

(A) 0GB
(B) 2GB
(C) 8GB
(D) 10GB

---

### Q2. [Ch01] (2점)

`free -h` 결과에서 서버의 **실제 메모리 여유**를 판단할 때 봐야 하는 컬럼은?

(A) free
(B) total
(C) available
(D) buff/cache

---

### Q3. [Ch02] (2점)

서버가 부팅된 지 3일 됐다. buff/cache가 전체 메모리의 60%를 차지하고 있다. 이 상태는?

(A) 비정상. 캐시가 너무 커서 즉시 회수해야 한다.
(B) 정상. OS가 빈 메모리를 캐시로 활용하는 건 설계된 동작이다.
(C) 비정상. 메모리 누수 가능성이 높다.
(D) 정상. 하지만 수동으로 캐시를 비워주는 게 좋다.

---

### Q4. [Ch03] (2점)

Swap이 발생하는 조건으로 **가장 정확한** 것은?

(A) Free가 0GB가 되면 Swap이 시작된다.
(B) buff/cache가 가득 차면 Swap이 시작된다.
(C) available이 부족해서 물리 메모리만으로 감당이 안 될 때 Swap이 시작된다.
(D) Used가 Total의 50%를 넘으면 Swap이 시작된다.

---

### Q5. [Ch04] (2점)

`vm.swappiness` 값이 **0**이면 어떤 의미야?

(A) Swap을 절대 안 한다. 어떤 상황에서도.
(B) Swap을 완전히 비활성화한다.
(C) 가능한 한 Swap을 피하고, 극한 상황에서만 Swap한다.
(D) buff/cache를 회수하지 않고 바로 Swap한다.

---

### Q6. [Ch04] (2점)

Linux의 `vm.swappiness` 기본값은?

(A) 0
(B) 10
(C) 30
(D) 60

---

### Q7. [Ch05] (2점)

Swap 사용량이 한번 올라간 후 시스템 부하가 줄어들었는데도 Swap이 안 줄어드는 이유는?

(A) 커널 버그 때문이다.
(B) Swap에 나간 데이터를 다시 RAM으로 가져오는 건 비용이 크기 때문에 OS가 적극적으로 하지 않는다.
(C) Swap 파티션은 한번 쓰면 포맷해야 비워진다.
(D) 프로세스가 종료되어야만 Swap이 해제된다.

---

### Q8. [Ch06] (2점)

백업 작업 중 대량의 파일을 순차적으로 읽을 때, 메모리에서 가장 먼저 일어나는 변화는?

(A) Used 증가
(B) Swap 증가
(C) buff/cache 증가, Free 감소
(D) available 증가

---

### Q9. [Ch03] (2점)

Swap이 위험한 근본적인 이유로 **가장 핵심적인** 것은?

(A) Swap 파티션이 가득 차면 시스템이 꺼진다.
(B) 디스크는 RAM보다 수천~수만 배 느려서, Swap I/O가 시스템 전체를 느리게 만든다.
(C) Swap을 사용하면 데이터가 손상될 수 있다.
(D) Swap은 CPU 사용률을 급격히 올린다.

---

### Q10. [Ch07] (2점)

WAS 서버의 모니터링 데이터에서 **Swap 사용량이 서서히 계단식으로 증가**하는 패턴이 보인다. 이 패턴이 의미하는 것은?

(A) 한 번의 대규모 메모리 할당으로 Swap이 발생했다.
(B) 주기적인 작업(백업, 배치 등)이 반복적으로 메모리를 압박하고, Swap은 비가역적이라 누적된다.
(C) 메모리 누수가 발생하고 있다.
(D) Swap 파티션이 손상되어 정상적으로 해제되지 않는다.

---

## Part 2. 단답형 (Q11~Q18, 각 3점)

!!! info "답변 규칙"
    정확한 값, 명령어, 용어를 적을 것. 설명 필요 없음.

---

### Q11. [Ch01] (3점)

OS 메모리의 기본 공식을 써라.

```
Total = _____ + _____ + _____
```

---

### Q12. [Ch01] (3점)

`available`의 계산 공식을 써라.

```
available = _____ + _____
```

---

### Q13. [Ch02] (3점)

OS가 buff/cache를 회수하는 시점은 언제인가? 한 문장으로.

---

### Q14. [Ch04] (3점)

`vm.swappiness`의 **운영 서버 권장값**은 얼마이고, 이 값을 **재부팅 후에도 유지**하려면 어떤 파일을 수정해야 하는가?

답: 권장값 = `_____`, 파일 = `_____`

---

### Q15. [Ch05] (3점)

Swap 비가역성 때문에 Swap 사용량을 강제로 비우려면 어떤 명령어 조합을 써야 하는가?

```bash
_____
```

---

### Q16. [Ch06] (3점)

백업 작업이 메모리에 영향을 주는 인과관계를 4단어 이내의 화살표 체인으로 표현해라.

```
_____ → _____ → _____ → _____
```

---

### Q17. [Ch03] (3점)

Swap 영역은 보통 어디에 존재하는가? (두 가지 형태)

---

### Q18. [Ch07] (3점)

서버 메모리 상태를 **실시간으로 반복 모니터링**하는 명령어를 써라. (2초 간격)

```bash
_____
```

---

## Part 3. 코드 작성/분석 (Q19~Q25, 각 5점)

---

### Q19. [Ch01] (5점)

아래 `free -h` 출력을 보고 물음에 답해라.

```
              total        used        free      shared  buff/cache   available
Mem:           15Gi        12Gi       0.2Gi       1.0Gi       3.0Gi       2.5Gi
Swap:         4.0Gi        2.8Gi       1.2Gi
```

**(a)** 이 서버의 메모리 상태는 위험한가? 이유는?

**(b)** 새 프로그램이 3GB 메모리를 요청하면 어떻게 되는가?

---

### Q20. [Ch04] (5점)

아래 명령어를 실행한 결과를 예측해라.

```bash
# 현재 상태
cat /proc/sys/vm/swappiness
# 출력: 60

# 변경
sysctl -w vm.swappiness=10

# 확인
cat /proc/sys/vm/swappiness
```

**(a)** 마지막 `cat` 출력값은?

**(b)** 서버를 재부팅하면 `vm.swappiness` 값은 얼마가 되는가? 왜?

**(c)** 영구 적용하려면 어떻게 해야 하는가? 명령어 또는 파일 수정 내용을 써라.

---

### Q21. [Ch05, Ch06] (5점)

아래 시나리오를 시간순으로 분석해라.

```
[14:00] 서버 메모리: Used 5GB, buff/cache 8GB, Free 2GB, Swap 0GB
[14:30] 백업 시작 (100GB 파일 순차 읽기)
[15:00] 서버 메모리: Used 5GB, buff/cache 9.5GB, Free 0.3GB, Swap 0.2GB
[15:30] 백업 완료
[16:00] 서버 메모리: Used 5GB, buff/cache 9GB, Free 0.8GB, Swap 0.2GB
```

**질문**: 16:00에 백업이 끝났는데 왜 Swap이 0.2GB로 그대로야? buff/cache는 왜 9GB나 차지하고 있어?

---

### Q22. [Ch02] (5점)

아래 두 서버를 보고 **어느 쪽이 더 건강한 상태인지** 판단하고 근거를 써라.

| | total | used | free | buff/cache | available |
|---|---|---|---|---|---|
| **서버 X** | 16Gi | 4Gi | 10Gi | 2Gi | 11.5Gi |
| **서버 Y** | 16Gi | 4Gi | 1Gi | 11Gi | 11Gi |

---

### Q23. [Ch07] (5점)

WAS02 서버의 아래 모니터링 데이터를 보고 장애 원인을 분석해라.

```
[월요일 02:00] Swap: 0.5GB  (백업 실행)
[월요일 06:00] Swap: 0.8GB  (백업 완료)
[화요일 02:00] Swap: 0.8GB  (백업 실행)
[화요일 06:00] Swap: 1.2GB  (백업 완료)
[수요일 02:00] Swap: 1.2GB  (백업 실행)
[수요일 06:00] Swap: 1.6GB  (백업 완료)
[목요일]       WAS 응답 지연 발생
```

**(a)** 이 데이터에서 보이는 패턴은 뭐야?

**(b)** 목요일에 응답 지연이 발생한 근본 원인은?

**(c)** 해결 방안 2가지를 제시해라.

---

### Q24. [Ch03, Ch04] (5점)

아래 두 서버의 설정과 상태를 비교해라.

```
[서버 A] vm.swappiness=60, Used=10GB, available=3GB, Swap=0.5GB
[서버 B] vm.swappiness=1,  Used=10GB, available=3GB, Swap=0.1GB
```

**질문**: 같은 Used, 같은 available인데 왜 Swap 사용량이 다른가? `vm.swappiness`가 이 차이에 어떻게 기여했는지 설명해라.

---

### Q25. [Ch01, Ch02, Ch03] (5점)

`free -h` 결과만 보고 **이 서버에 곧 Swap이 발생할 것인지** 예측해라. 판단 근거를 구체적으로 써라.

```
              total        used        free      shared  buff/cache   available
Mem:           8Gi         6.8Gi       0.1Gi       0.5Gi       1.1Gi       0.8Gi
Swap:         2.0Gi        0.0Gi       2.0Gi
```

---

## Part 4. 서술형 (Q26~Q30)

!!! warning "채점 기준"
    핵심 개념 + 인과관계 + 근거가 있어야 점수를 받을 수 있다.
    "그냥 그래요", "원래 그래요"는 0점이야.

---

### Q26. [Ch02] (4점)

**"buff/cache가 많으면 메모리 부족인가?"**

이 질문에 대해 맞다/틀리다를 밝히고, 근거를 들어 설명해라. buff/cache의 존재 이유와 회수 메커니즘을 포함할 것.

---

### Q27. [Ch05] (4점)

**Swap의 비가역성**을 설명해라.

다음을 반드시 포함할 것:

- 비가역적이라는 게 정확히 무슨 뜻인지
- OS가 Swap을 적극적으로 해제하지 않는 이유
- 이 특성이 운영에 미치는 영향

---

### Q28. [Ch06, Ch07] (5점)

**"새벽 백업이 WAS 성능 저하의 원인이 될 수 있다"**는 주장의 인과관계를 처음부터 끝까지 설명해라.

다음 단계를 모두 포함할 것:
디스크 스캔 → buff/cache → Free → available → Swap → 성능

---

### Q29. [Ch04] (4점)

`vm.swappiness=60`인 서버와 `vm.swappiness=1`인 서버가 있다. 둘 다 available이 1GB로 동일할 때, 메모리 압박 상황에서 두 서버의 **OS 행동 차이**를 설명해라.

---

### Q30. [Ch01~Ch07 통합] (4점)

네가 WAS 운영 서버를 새로 셋업한다고 해봐. 메모리 관점에서 **Swap 폭증 장애를 예방**하기 위한 설정과 운영 방안을 4가지 이상 제시해라. 각 항목마다 **왜 그렇게 해야 하는지** 이유를 반드시 포함할 것.

---

## 시험 끝

!!! quote "채점 전에"
    다 풀었으면 [09_빠싺_최종시험_정답.md](09_빠싺_최종시험_정답.md) 가서 채점해.

    80점 이상이면 합격. 미만이면? **01장부터 다시.**

    "Not quite my tempo? 다시."
