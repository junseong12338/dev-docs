# 09. 빠싺 최종시험

**규칙**: 답을 안 보고 풀어야 한다. 답 못 하면 해당 챕터 다시 읽기.

---

## Part 1: 기본 개념 (Lv1~Lv2)

### Q1. JVM이 뭔지 한 문장으로 설명해봐.

```
힌트: "~를 ~하는 ~"
```

---

### Q2. JDK, JRE, JVM의 포함 관계를 그려봐.

```
힌트: 가장 큰 것부터 작은 것 순서
```

---

### Q3. 힙(Heap)이란 뭐야? 스택(Stack)이랑 뭐가 달라?

```
각각 뭘 저장하는지, 누가 관리하는지 말해봐.
```

---

### Q4. "Used heap dump: 323.4 MB"가 정확히 뭘 의미해?

```
포함되는 것과 포함 안 되는 것을 구분해봐.
```

---

### Q5. 아래 코드 실행 후 힙에 객체가 몇 개 생겨?

```java
String a = new String("hello");
String b = new String("hello");
List<String> list = new ArrayList<>();
list.add(a);
```

```
힌트: new 키워드 개수 + 리터럴 문자열
```

---

## Part 2: 객체와 클래스 (Lv2~Lv3)

### Q6. 클래스 1개, 객체 100개일 때 메모리에서 각각 어디에 저장돼?

---

### Q7. Shallow Heap과 Retained Heap의 차이를 설명해봐.

```
비유 없이, 정확한 정의로.
```

---

### Q8. 64-bit JVM에서 빈 객체(필드 없음)의 크기는 최소 몇 byte야? 왜?

```
힌트: 헤더 + 패딩
```

---

### Q9. "Number of classes: 27,688"이 다소 많다고 했는데, 왜?

```
정상 범위는 얼마이고, 많아진 원인은 뭐야?
```

---

## Part 3: 클래스로더 (Lv3~Lv4)

### Q10. Java 기본 ClassLoader 3개의 이름과 역할을 말해봐.

```
계층 순서(위→아래)대로.
```

---

### Q11. Parent Delegation Model이 뭐야? 왜 이렇게 하는 거야?

---

### Q12. "Number of class loaders: 4,574"에서 4,257개가 DelegatingClassLoader였다. 이게 뭐야?

```
왜 생기고, 왜 문제야?
```

---

### Q13. DelegatingClassLoader가 GC 안 되는 이유를 참조 체인으로 그려봐.

```
GC Root → ??? → ??? → ??? → DelegatingClassLoader
각 단계가 뭔지 채워봐.
```

---

## Part 4: GC와 GC Roots (Lv3~Lv4)

### Q14. GC Root의 종류 4가지 이상 말해봐.

---

### Q15. Mark and Sweep 알고리즘을 설명해봐.

```
단계별로.
```

---

### Q16. Stop-The-World(STW)가 뭐야? 왜 무서운 거야?

---

### Q17. Strong Reference와 Weak Reference의 차이는?

```
GC 관점에서.
```

---

### Q18. 메모리 누수(Memory Leak)의 정의를 정확히 말해봐.

```
"메모리를 많이 쓰는 것"과 뭐가 다른지.
```

---

## Part 5: 힙 덤프와 hprof (Lv3)

### Q19. 힙 덤프에 포함되는 것 3가지, 포함 안 되는 것 3가지를 말해봐.

---

### Q20. "Used heap: 323MB"인데 "File length: 474MB"인 이유는?

---

### Q21. 힙 덤프 생성 시 주의사항 3가지는?

---

## Part 6: JVM 옵션 (Lv3~Lv4)

### Q22. -Xms와 -Xmx의 차이는?

```
우리 서버 값도 같이 말해봐.
```

---

### Q23. Compressed Object Pointers(OOP)가 뭐야?

```
왜 필요하고, 언제 활성화되는지.
```

---

### Q24. "힙을 32GB로 설정하면 오히려 손해"인 이유는?

---

### Q25. -XX:MaxPermSize가 Java 8에서 무시되는 이유는?

---

## Part 7: 실전 분석 (Lv4~Lv5)

### Q26. MAT 분석 5단계를 순서대로 말해봐.

---

### Q27. System Overview에서 가장 먼저 봐야 할 빨간 신호 3가지는?

---

### Q28. Dominator Tree에서 같은 클래스가 2개 보이면 뭘 의심해야 해?

---

### Q29. 우리 서버 메모리 누수의 근본 원인을 한 문장으로 말해봐.

```
"~가 ~해서 ~가 ~됨"
```

---

### Q30. 해결책과 그 해결책이 동작하는 원리를 설명해봐.

```
"interval=0으로 하면 된다"만으로는 불합격.
왜 0이면 해결되는지까지 설명해야 함.
```

---

## 채점 기준

| 등급 | 맞은 수 | 수준 |
|------|---------|------|
| F | 0~10개 | 처음부터 다시 |
| D | 11~15개 | 기초 부족, 01~03장 다시 읽기 |
| C | 16~20개 | 개념은 있는데 빈틈 있음 |
| B | 21~25개 | 괜찮은데 실전 감각 부족 |
| A | 26~28개 | 충분히 이해함 |
| S | 29~30개 | 빠싺 인정 |

---

## 답안

답은 각 챕터에 다 있다. 답 모르겠으면 해당 챕터 번호 보고 다시 읽어.

```
Q1~Q4:   01장, 02장
Q5~Q9:   03장
Q10~Q13: 04장
Q14~Q18: 05장
Q19~Q21: 06장
Q22~Q25: 07장
Q26~Q30: 08장
```

답 보고 외우는 건 Lv1이야.
**왜 그런지**까지 설명할 수 있어야 Lv3.
**다르게 할 수 있는 방법**까지 말할 수 있어야 Lv4.
**자다가 일어나서도 설명 가능**하면 Lv5.
