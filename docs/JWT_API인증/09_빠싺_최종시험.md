# 09. 빠싺 최종시험 - Black Hole

---

!!! danger "최종시험"
    "There are no two words in the English language
    more harmful than 'good job'."

    01~08 전 범위. 30문제. 100점 만점.
    여기서 80점 못 넘기면 처음부터 다시 읽어.

## 시험 규칙

| 항목 | 규칙 |
|------|------|
| **합격 기준** | 80점 이상 |
| **시간 제한** | 60분 |
| **오픈북** | 금지. 머리에 없으면 모르는 거야. |
| **검색** | 금지. 구글 켜는 순간 불합격. |
| **부분 점수** | 서술형만 부분 점수 있음 |
| **총점** | 100점 |

---

## Part 1: 객관식 (10문제, 각 2점 = 20점)

### Q1. [01장] (2점)

JWT는 어떤 방식으로 인증 정보를 관리해?

(A) 서버 메모리에 세션으로 저장
(B) 토큰 자체에 정보를 담아서 클라이언트가 보관
(C) 데이터베이스에 토큰 정보를 저장
(D) 쿠키에 사용자 ID/PW를 저장

---

### Q2. [02장] (2점)

Base64는 뭐야?

(A) 암호화 알고리즘
(B) 해싱 알고리즘
(C) 인코딩 방식
(D) 서명 알고리즘

---

### Q3. [02장] (2점)

다음 중 "단방향"인 것은?

(A) AES-256 암호화
(B) Base64 인코딩
(C) HMAC-SHA256 해싱
(D) RSA 암호화

---

### Q4. [03장] (2점)

JWT에서 Payload를 변조했을 때 LMS 서버가 이를 감지하는 방법은?

(A) Payload를 복호화해서 원본과 비교
(B) Header의 알고리즘 정보를 확인
(C) SECRET_KEY로 서명을 다시 만들어서 비교
(D) Base64 디코딩해서 형식 검증

---

### Q5. [03장] (2점)

JWT Payload에 넣으면 **안 되는** 것은?

(A) 앱 키 (APP_KEY)
(B) 토큰 만료 시각 (exp)
(C) 사용자 비밀번호
(D) 토큰 발급 시각 (iat)

---

### Q6. [04장] (2점)

HTTP 요청에서 JWT를 전달하는 올바른 형식은?

(A) `Cookie: JWT=eyJhbGci...`
(B) `Authorization: eyJhbGci...`
(C) `Authorization: Bearer eyJhbGci...`
(D) `X-Token: eyJhbGci...`

---

### Q7. [05장] (2점)

NexClass → LMS 통신에서 세션 대신 JWT를 쓰는 가장 큰 이유는?

(A) JWT가 세션보다 빠르니까
(B) 서버↔서버 통신이라 브라우저/쿠키가 없으니까
(C) JWT가 세션보다 보안이 좋으니까
(D) 세션은 Java 17에서 지원 안 하니까

---

### Q8. [06장] (2점)

AES-256에서 "대칭키"라는 말의 의미는?

(A) 암호화와 복호화 알고리즘이 같다
(B) 암호화 키와 복호화 키가 같다
(C) 블록 크기와 키 크기가 같다
(D) 입력과 출력 크기가 같다

---

### Q9. [07장] (2점)

JWT의 가장 큰 보안 약점은?

(A) 토큰이 너무 길어서 네트워크 부담
(B) 서명 알고리즘이 약해서 위조 가능
(C) 탈취된 토큰을 서버에서 즉시 무효화하기 어렵다
(D) Payload가 암호화되지 않아서 데이터 유출

---

### Q10. [08장] (2점)

동기화 순서가 term → crs → users → crsUsers인 이유는?

(A) 알파벳 순서
(B) 데이터 크기 순서 (작은 것부터)
(C) 의존성 순서 (참조하는 데이터가 먼저 있어야 함)
(D) LMS API가 이 순서로만 응답하니까

---

## Part 2: 단답형 (8문제, 각 3점 = 24점)

### Q11. [01장] (3점)

세션 방식에서 서버가 3대로 늘어나면 발생하는 문제를 한 줄로 설명해봐.

---

### Q12. [02장] (3점)

JWT에서 인코딩, 해싱, 암호화가 각각 어디에 쓰이는지 한 줄씩 써봐.

---

### Q13. [03장] (3점)

APP_KEY와 SECRET_KEY의 차이를 한 줄씩 설명해봐.

---

### Q14. [03장] (3점)

Unix 타임스탬프가 뭐야? 왜 JWT에서 이걸 써?

---

### Q15. [04장] (3점)

`Jwts.builder().signWith(...)` 에서 signWith가 JWT 3조각 중 어느 부분을 만들어?

---

### Q16. [05장] (3점)

LMS에서 401 응답이 왔어. 가능한 원인 3가지를 써봐.

---

### Q17. [06장] (3점)

IV (Initialization Vector)가 뭐고, CBC 모드에서 왜 필요해?

---

### Q18. [07장] (3점)

서버↔서버 통신에서 Refresh Token이 필요 없는 이유를 써봐.

---

## Part 3: 코드 작성/분석 (7문제, 각 5점 = 35점)

### Q19. [04장] (5점)

다음 JWT 생성 코드에서 각 줄이 JWT 3조각 중 어디에 해당하는지 써봐.

```java
Jwts.builder()
    .claim("appKey", "knu-lms-2026")   // 이건 ____
    .issuedAt(new Date())               // 이건 ____
    .signWith(key, Jwts.SIG.HS256)      // 이건 ____
    .compact();                          // 이건 ____
```

---

### Q20. [06장] (5점)

다음 AES 복호화 코드의 각 단계가 뭘 하는지 설명해봐.

```java
Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");  // ①
cipher.init(Cipher.DECRYPT_MODE, keySpec, ivSpec);            // ②
byte[] decoded = Base64.getDecoder().decode(encrypted);       // ③
return new String(cipher.doFinal(decoded));                   // ④
```

---

### Q21. [05장] (5점)

다음 HTTP 요청의 각 줄이 뭘 하는지 설명해봐.

```
POST /vc/api/sync/term?orgCd=ORG0000001 HTTP/1.1
Authorization: Bearer eyJhbGci...
Content-Type: application/json

{"pageNumber": 0, "pageSize": 1000}
```

---

### Q22. [08장] (5점)

다음 코드가 잘못된 이유를 찾아서 수정해봐.

```java
// 잘못된 코드
String token = generateToken();  // 토큰 1번 생성

for (int page = 0; page < 52; page++) {
    // 52페이지 동안 같은 토큰 재사용
    String response = callLmsApi("users", page, token);
    // ... 처리
}
```

힌트: 만료 시간은 30분이야.

---

### Q23. [02장] (5점)

다음 보기에서 올바른 것과 틀린 것을 골라 이유를 설명해봐.

```
(A) "JWT의 Payload는 암호화되어 있어서 안전하다"
(B) "HMAC-SHA256 해시값에서 원본을 복원할 수 없다"
(C) "AES-256은 비대칭키 암호화이다"
(D) "Base64 디코딩에는 키가 필요하다"
```

---

### Q24. [08장] (5점)

NexClass→LMS 데이터 동기화에서 JWT와 AES가 각각 어느 방향에서 어떤 역할을 하는지 다이어그램으로 그려봐.

---

### Q25. [06장] (5점)

`BadPaddingException`이 발생했어. 원인을 3가지 말하고, 각각 어떻게 확인하는지 써봐.

---

## Part 4: 서술형 (5문제, 배점 다름 = 21점)

### Q26. [01장, 05장] (4점)

"API 인증"이 뭔지, 왜 필요한지, JWT가 어떻게 이를 해결하는지 서술해봐. (5문장 이내)

---

### Q27. [03장, 07장] (4점)

SECRET_KEY가 노출되면 어떤 일이 벌어지는지, 그래서 어떻게 관리해야 하는지 서술해봐. (5문장 이내)

---

### Q28. [02장, 06장] (5점)

JWT에서 Signature가 "무결성"을 보장한다고 했어. "무결성"과 "기밀성"의 차이를 설명하고, JWT에서 각각을 어떤 기술이 담당하는지 서술해봐.

---

### Q29. [07장] (4점)

JWT 만료 시간을 5분으로 설정하는 것과 24시간으로 설정하는 것의 트레이드오프를 분석해봐. 우리 프로젝트에서는 어떻게 설정했고 왜 그렇게 했는지도 포함해서.

---

### Q30. [08장] (4점)

NexClass에서 users(5만 명+) 동기화 시 페이징이 필요한 이유를 설명하고, 52페이지를 도는 동안 JWT 만료 문제를 어떻게 해결할 수 있는지 2가지 방법을 제시해봐.

---

!!! quote "마무리"
    수고했어.
    80점 넘겼으면 다음으로.
    못 넘겼으면 처음부터 다시.

    "Not quite my tempo? 다시."
