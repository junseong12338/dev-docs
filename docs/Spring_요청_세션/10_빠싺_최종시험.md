# 10. 빠싺 최종시험

**"Were you rushing or were you dragging?"**

---

## 시험 안내

| 항목 | 내용 |
|------|------|
| **시간** | 60분 |
| **오픈북** | 금지 |
| **합격 기준** | 80점 이상 |
| **총점** | 100점 |

| 유형 | 문제 수 | 배점 | 소계 |
|------|---------|------|------|
| 객관식 | 10문제 | 2점 | 20점 |
| 단답형 | 8문제 | 3점 | 24점 |
| 코드 분석 | 7문제 | 5점 | 35점 |
| 서술형 | 5문제 | ~4점 | 21점 |

!!! danger "규칙"
    - 문제를 끝까지 읽고 답해라. 급발진 금지.
    - "아는 것 같은데..." 수준은 0점이다. 정확하게 써라.
    - 서술형은 핵심만 간결하게. 소설 쓰지 마라.

---

## 객관식 (10문제 x 2점 = 20점)

### Q1. [01장] (2점)

브라우저에서 서버까지 요청이 도달하는 순서로 올바른 것은?

A. Tomcat → Nginx → DispatcherServlet → Controller
B. DispatcherServlet → Nginx → Tomcat → Controller
C. Nginx → Tomcat → DispatcherServlet → Controller
D. Controller → DispatcherServlet → Tomcat → Nginx

### Q2. [02장] (2점)

DispatcherServlet의 역할로 **틀린** 것은?

A. 모든 요청의 진입점 역할 (Front Controller)
B. HandlerMapping을 통해 적절한 컨트롤러를 찾는다
C. 비즈니스 로직을 직접 실행한다
D. ViewResolver를 통해 뷰를 결정한다

### Q3. [03장] (2점)

인터셉터의 메서드 실행 순서로 올바른 것은?

A. postHandle → preHandle → Controller → afterCompletion
B. preHandle → Controller → afterCompletion → postHandle
C. preHandle → Controller → postHandle → afterCompletion
D. Controller → preHandle → postHandle → afterCompletion

### Q4. [04장] (2점)

JSESSIONID 쿠키에 대한 설명으로 **틀린** 것은?

A. 서버가 생성하여 브라우저에 전달한다
B. 사용자의 이름, 학번 등 실제 데이터가 들어있다
C. 서버 메모리(또는 Redis)의 세션을 찾는 키 역할을 한다
D. 브라우저가 매 요청마다 자동으로 서버에 보낸다

### Q5. [04장] (2점)

session-timeout이 120분이다. 사용자가 10:00에 로그인, 10:30에 강의 조회, 이후 활동 없음. 세션이 만료되는 시각은?

A. 12:00
B. 12:30
C. 10:30
D. 11:00

### Q6. [05장] (2점)

Redis를 세션 저장소로 사용하는 주된 이유는?

A. MySQL보다 데이터를 안전하게 보관하기 위해
B. 여러 대의 WAS에서 같은 세션을 공유하기 위해
C. 세션 데이터를 영구적으로 저장하기 위해
D. 브라우저가 직접 Redis에 접속하기 위해

### Q7. [06장] (2점)

SessionBrokenException과 AccessDeniedException의 차이로 올바른 것은?

A. 둘 다 세션 만료 시 발생한다
B. SessionBroken은 인증 실패, AccessDenied는 인가 실패
C. SessionBroken은 인가 실패, AccessDenied는 인증 실패
D. 둘 다 권한 부족 시 발생한다

### Q8. [07장] (2점)

세션 만료 후 사용자가 페이지를 요청했을 때 일어나는 일로 **틀린** 것은?

A. 브라우저는 여전히 JSESSIONID 쿠키를 보낸다
B. Redis에서 해당 세션을 찾지 못한다
C. 서버가 세션 만료 이벤트를 미리 감지하여 알림을 보낸다
D. SessionBrokenException이 발생하고 로그인 페이지로 리다이렉트된다

### Q9. [08장] (2점)

e.printStackTrace()의 문제점으로 **틀린** 것은?

A. 로그 레벨 제어가 불가능하다
B. 파일 로테이션이 지원되지 않는다
C. Logger보다 출력 속도가 빠르다
D. 타임스탬프가 포함되지 않는다

### Q10. [09장] (2점)

WAS02 사고에서 catalina.out이 4.4GB까지 커진 근본 원인은?

A. Redis 서버 장애
B. Nginx 설정 오류
C. e.printStackTrace()로 모든 예외의 스택 트레이스를 출력한 것
D. Tomcat 메모리 누수

---

## 단답형 (8문제 x 3점 = 24점)

### Q11. [01장] (3점)

Web Server(Nginx)가 처리하는 요청과 WAS(Tomcat)가 처리하는 요청의 종류를 각각 쓰시오.

### Q12. [02장] (3점)

ViewResolver의 prefix가 `/WEB-INF/views/`이고 suffix가 `.jsp`일 때, 컨트롤러가 `"lms/score/scoreList"`를 반환하면 최종 JSP 경로는?

### Q13. [03장] (3점)

인터셉터의 `preHandle()`이 `false`를 반환하면 어떻게 되는지 쓰시오.

### Q14. [04장] (3점)

HTTP가 무상태(Stateless)인 이유를 한 문장으로 쓰시오.

### Q15. [05장] (3점)

우리 프로젝트의 Redis 설정 3가지(host, port, expire)의 값을 쓰시오.

### Q16. [06장] (3점)

`SecurityUtil.authorizationCheckRunner()` 메서드가 무의미한 이유를 한 문장으로 쓰시오.

### Q17. [07장] (3점)

세션이 만료될 때 서버에 "만료됐다"는 이벤트가 전달되는가? 서버가 만료를 알게 되는 시점은 언제인가?

### Q18. [08장] (3점)

`e.printStackTrace()`의 출력이 Tomcat에서 최종적으로 기록되는 파일 이름은?

---

## 코드 분석 (7문제 x 5점 = 35점)

### Q19. [03장] (5점)

다음 코드를 분석하시오. 이 catch 블록이 하는 일을 정확히 설명하시오.

```java
try {
    SecurityUtil.authorizationCheckRunner(request, response);
} catch(Exception e) {
    e.printStackTrace();
    throw e;
}
```

### Q20. [06장] (5점)

다음 코드를 분석하시오. 이 메서드가 존재하는 의미가 있는지, 없다면 왜 없는지 설명하시오.

```java
public static void authorizationCheckRunner(
        HttpServletRequest request,
        HttpServletResponse response) throws Exception {
    try {
        SecurityUtil.authorizationCheck(request, response);
    } catch (Exception ex) {
        throw ex;
    }
}
```

### Q21. [06장] (5점)

다음 코드에서 `SessionBrokenException`이 발생하는 조건을 설명하시오.

```java
if(Action.matches(".*Lect/.*") || Action.matches(".*Open/.*")) {
    if(ValidationUtils.isEmpty(UserBroker.getUserNo(request))) {
        throw new SessionBrokenException("system.fail.session.expire");
    }
}
```

### Q22. [04장] (5점)

다음 코드가 하는 일을 설명하시오. 반환값이 빈 문자열인 경우 어떤 의미인지도 쓰시오.

```java
public static final String getUserNo(HttpServletRequest request) {
    return getSessionValue(request, Constants.LOGIN_USERNO);
}
```

### Q23. [08장] (5점)

수정 전과 수정 후 코드를 비교하시오. `SessionBrokenException`을 별도로 잡는 이유를 설명하시오.

```java
// 수정 전
catch(Exception e) {
    e.printStackTrace();
    throw e;
}

// 수정 후
catch(SessionBrokenException e) {
    throw e;
} catch(Exception e) {
    log.error("Authorization check failed: {}", e.getMessage());
    throw e;
}
```

### Q24. [06장] (5점)

다음 예외 계층 구조를 보고, SessionBrokenException이 첫 번째 catch에 잡히는 이유를 설명하시오.

```
Exception
  └── MediopiaDefineException
        └── AuthorityException
              └── SessionBrokenException
```

```java
catch(SessionBrokenException e) {   // 여기에 잡힌다
    throw e;
} catch(Exception e) {              // 여기에 안 잡힌다
    log.error("...", e.getMessage());
    throw e;
}
```

### Q25. [09장] (5점)

다음 명령어가 하는 일을 단계별로 설명하시오.

```bash
grep -oP '\w+Exception' /data/tomcatlogs/catalina.out | sort | uniq -c | sort -rn
```

---

## 서술형 (5문제, 21점)

### Q26. [05장] (4점)

HUB에서 로그인한 사용자가 LMS에서도 로그인 상태를 유지할 수 있는 이유를 Redis의 역할을 중심으로 설명하시오. (3-5문장)

### Q27. [07장] (4점)

"세션 만료는 에러가 아니라 정상 흐름이다"라는 주장의 근거를 3가지 쓰시오.

### Q28. [09장] (5점)

WAS02 사고의 연쇄 반응을 시작(e.printStackTrace())부터 끝(서버 무응답)까지 5단계로 서술하시오. 각 단계가 다음 단계를 어떻게 유발했는지 포함할 것.

### Q29. [08장] (4점)

프로덕션 환경에서 e.printStackTrace() 대신 Logger를 써야 하는 이유를 3가지 쓰시오.

### Q30. [09장] (4점)

WAS02 사고를 사전에 방지하려면 어떤 모니터링 지표를 감시해야 했는지 3가지와, 각각의 임계치 기준을 제시하시오.

---

!!! warning "시험 끝"
    다 풀었으면 처음부터 다시 한번 확인해라. "다 맞은 것 같은데"가 가장 위험하다.

    정답과 해설은 **11장**에 있다.
