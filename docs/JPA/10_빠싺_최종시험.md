# 10. 빠싺 최종시험 - Black Hole

---

!!! danger ""

    *"There are no two words in the English language
    more harmful than 'good job'."*

    01~09 전 범위. 30문제. 100점 만점.
    여기서 80점 못 넘기면 처음부터 다시 읽어.

## 시험 규칙

| 항목 | 규칙 |
|------|------|
| **합격 기준** | 80점 이상 |
| **시간 제한** | 60분 |
| **오픈북** | 금지. 머리에 없으면 모르는 거야. |
| **검색** | 금지. 구글 켜는 순간 불합격. |
| **부분 점수** | 서술형만 부분 점수 있음 |
| **총점** | 100점 |

---

## 객관식 (10문제 × 2점 = 20점)

### Q1. [Ch01] (2점)
JPA와 Hibernate의 관계로 올바른 것은?

(A) JPA가 Hibernate를 상속한다
(B) Hibernate가 JPA 스펙을 구현한다
(C) JPA와 Hibernate는 동일한 라이브러리다
(D) Hibernate는 JPA 없이 동작할 수 없다

---

### Q2. [Ch02] (2점)
JPA 엔티티에 @NoArgsConstructor가 필요한 이유는?

(A) Builder 패턴을 위해
(B) JPA가 리플렉션으로 빈 객체를 먼저 생성하기 때문
(C) Lombok이 요구하기 때문
(D) Spring 컨테이너가 빈으로 등록하기 위해

---

### Q3. [Ch03] (2점)
JpaRepository의 save() 메서드가 INSERT를 실행하는 조건은?

(A) 엔티티의 모든 필드가 null일 때
(B) 엔티티의 PK가 null이거나 DB에 없을 때
(C) 엔티티에 @GeneratedValue가 있을 때
(D) save()는 항상 INSERT만 실행한다

---

### Q4. [Ch04] (2점)
@ManyToOne 관계에서 "연관관계의 주인"은?

(A) @OneToMany가 붙은 쪽
(B) PK가 있는 쪽
(C) FK가 있는 쪽
(D) mappedBy가 붙은 쪽

---

### Q5. [Ch05] (2점)
영속성 컨텍스트의 변경 감지(Dirty Checking)가 동작하려면?

(A) entityManager.flush()를 호출해야 한다
(B) repository.save()를 반드시 호출해야 한다
(C) @Transactional 안에서 영속 상태 엔티티여야 한다
(D) @PreUpdate가 있어야 한다

---

### Q6. [Ch06] (2점)
JPQL에서 올바른 쿼리는?

(A) SELECT * FROM TB_NEXCLASS_TERM
(B) SELECT t FROM Term t
(C) SELECT TERM_CD FROM Term
(D) SELECT t.TERM_CD FROM Term t

---

### Q7. [Ch07] (2점)
@Transactional이 같은 클래스 내부 호출에서 안 먹히는 이유는?

(A) 스프링이 내부 호출을 금지해서
(B) 프록시 기반이라 this 호출은 프록시를 안 거쳐서
(C) 같은 트랜잭션에 합류해서
(D) readOnly 설정 때문에

---

### Q8. [Ch08] (2점)
N+1 문제의 해결책으로 적절하지 않은 것은?

(A) fetch join
(B) @EntityGraph
(C) FetchType.EAGER로 변경
(D) @BatchSize

---

### Q9. [Ch09] (2점)
NexClass에서 Term.java에 @GeneratedValue가 없는 이유는?

(A) MariaDB가 지원 안 해서
(B) String 타입은 자동 생성이 안 돼서
(C) LMS에서 학기코드(PK)가 넘어오기 때문에
(D) @PrePersist에서 직접 생성해서

---

### Q10. [Ch05] (2점)
같은 트랜잭션에서 findById("A")를 2번 호출하면?

(A) SQL 2번 실행
(B) SQL 1번 실행 (1차 캐시)
(C) 예외 발생
(D) 두 번째는 null 반환

---

## 단답형 (8문제 × 3점 = 24점)

### Q11. [Ch01] (3점)
MyBatis에서 INSERT/UPDATE를 따로 만드는데, JPA에서 같은 일을 하는 메서드 이름은?

---

### Q12. [Ch02] (3점)
PK 필드를 `private long syncId` (원시 타입)로 선언하면 생기는 문제를 한 문장으로.

---

### Q13. [Ch02] (3점)
@Column(name = "CREATED_AT", updatable = false)에서 updatable = false의 의미는?

---

### Q14. [Ch03] (3점)
findByHostStatus("ACTIVE")가 자동 생성하는 WHERE 절은?

---

### Q15. [Ch04] (3점)
NexClass에서 Course.java가 Term과 @ManyToOne 연관관계를 안 쓰고 String termCd만 저장한 원칙 이름은?

---

### Q16. [Ch06] (3점)
@Query로 UPDATE/DELETE 쿼리를 실행할 때 반드시 함께 써야 하는 어노테이션은?

---

### Q17. [Ch07] (3점)
@Transactional(readOnly = true)의 효과를 한 문장으로.

---

### Q18. [Ch08] (3점)
@ManyToOne의 기본 FetchType은? 왜 위험?

---

## 코드 작성/분석 (7문제 × 5점 = 35점)

### Q19. [Ch02] (5점)
아래 DDL에 맞는 JPA 엔티티의 PK 필드 부분을 작성해라 (@Id, @Column 포함).

```sql
CREATE TABLE TB_NEXCLASS_COURSE (
    COURSE_CD VARCHAR(40) PRIMARY KEY
);
```

---

### Q20. [Ch02] (5점)
아래 DDL에 맞는 JPA 엔티티의 PK 필드 부분을 작성해라.

```sql
CREATE TABLE TB_NEXCLASS_SYNC_LOG (
    SYNC_ID BIGINT AUTO_INCREMENT PRIMARY KEY
);
```

---

### Q21. [Ch03] (5점)
Course 엔티티(PK: String courseCd)를 위한 Repository 인터페이스를 완성해라.

```java
@Repository
public interface CourseRepository extends __________________ {

}
```

---

### Q22. [Ch03] (5점)
"사용자 유형(userType)으로 조회"하는 쿼리 메서드를 UserRepository에 추가해라. 메서드 시그니처만.

---

### Q23. [Ch05] (5점)
아래 코드에서 save()를 빼도 DB에 반영되는 이유를 설명하고, 빼도 되는 이유 or 안 되는 이유를 써라.

```java
@Transactional
public void updateTermName(String termCd, String newName) {
    Term term = termRepository.findById(termCd).orElseThrow();
    term.setTermName(newName);
    termRepository.save(term);  // ← 이거 빼도 되나?
}
```

---

### Q24. [Ch06] (5점)
아래 JPQL의 문제점을 찾고 수정해라.

```java
@Query("SELECT * FROM TB_NEXCLASS_HOST_ASSIGNMENT WHERE HOST_EMAIL = :email")
List<HostAssignment> findByEmail(@Param("email") String email);
```

---

### Q25. [Ch08] (5점)
학기 100개 조회 후 각 학기의 과목 목록에 접근할 때 N+1 문제가 발생한다. SQL이 총 몇 번 실행되는지, 그리고 fetch join으로 해결한 @Query를 써라.

---

## 서술형 (5문제 = 21점)

### Q26. [Ch01] (4점)
"JPA 쓰면 SQL 몰라도 된다"가 틀린 이유를 2가지 이상 서술해라.

---

### Q27. [Ch05] (4점)
영속성 컨텍스트의 1차 캐시와 변경 감지를 MyBatis와 비교해서 각각 설명해라.

---

### Q28. [Ch07] (4점)
NexClass createLesson()에서 @Transactional이 필요한 이유를 "호스트 배정 → Zoom 생성 → DB 저장" 흐름과 연결해서 설명해라.

---

### Q29. [Ch08] (5점)
N+1 문제가 뭔지 설명하고, MyBatis에서는 이 문제가 없는 이유, JPA에서의 해결 방법 2가지를 서술해라.

---

### Q30. [Ch09] (4점)
NexClass에서 엔티티 간 @ManyToOne 연관관계를 안 쓰고 String FK로만 저장한 설계 결정의 장단점을 각 2가지 이상 서술해라.

---

!!! danger ""

    끝. 정답은 11_빠싺_최종시험_정답.md에.
    80점 못 넘기면 01부터 다시. 변명 없다.
