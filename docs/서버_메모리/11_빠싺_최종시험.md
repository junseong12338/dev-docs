# 11. 빠싺 최종시험 - Black Hole

---

!!! danger "Black Hole"

    *"There are no two words in the English language more harmful than 'good job'."*

    01~10 전 범위. 30문제. 100점 만점. **여기서 80점 못 넘기면 처음부터 다시 읽어.**

## 시험 규칙

| 항목 | 규칙 |
|------|------|
| **합격 기준** | 80점 이상 |
| **시간 제한** | 60분 |
| **오픈북** | 금지. 머리에 없으면 모르는 거야. |
| **검색** | 금지. 구글 켜는 순간 불합격. |
| **부분 점수** | 서술형만 부분 점수 있음 |
| **총점** | 100점 |

## 배점 구성

| 유형 | 문항 수 | 문항당 점수 | 합계 | 비고 |
|------|---------|-------------|------|------|
| 객관식 (4지선다) | 10문제 | 2점 | 20점 | 개념 확인 |
| 단답형 | 8문제 | 3점 | 24점 | 정확한 이해 |
| 코드/명령어 분석 | 7문제 | 5점 | 35점 | 실전 능력 |
| 서술형 | 5문제 | 4~5점 | 21점 | 깊이 있는 이해 |
| **합계** | **30문제** | | **100점** | |

---

> **시작하기 전에.**
>
> 답을 "아마 이거일 걸?"로 쓰지 마. 근거 없는 답은 답이 아니야.
> 모르면 모른다고 써. 찍어서 맞히는 건 실력이 아니야.

---

## PART 1: 객관식 (10문제 x 2점 = 20점)

---

### Q1. [01장] (2점)

컴퓨터에서 1KB는 몇 바이트인가?

(A) 100 바이트
(B) 1,000 바이트
(C) 1,024 바이트
(D) 1,048 바이트

---

### Q2. [01장] (2점)

메모리 계층 구조에서 CPU에 가장 가까운 것부터 먼 것 순서로 올바르게 나열한 것은?

(A) RAM → 레지스터 → 캐시 → 디스크
(B) 레지스터 → 캐시 → RAM → 디스크
(C) 캐시 → 레지스터 → RAM → 디스크
(D) 레지스터 → RAM → 캐시 → 디스크

---

### Q3. [02장] (2점)

Linux에서 `ps -ef` 명령어의 PID 컬럼이 1인 프로세스는 무엇인가?

(A) 커널 자체
(B) init/systemd (최초 사용자 프로세스)
(C) bash 쉘
(D) 가장 먼저 실행된 Java 프로세스

---

### Q4. [03장] (2점)

`free -h` 출력에서 `available`이 의미하는 것은?

(A) 현재 아무것도 사용하지 않는 물리 메모리
(B) Swap 영역에서 사용 가능한 크기
(C) 새 프로세스가 Swap 없이 사용할 수 있는 예상 메모리 (free + 회수 가능한 캐시/버퍼)
(D) total에서 used를 뺀 값

---

### Q5. [04장] (2점)

프로세스가 `malloc(1GB)`를 호출했을 때, 즉시 물리 메모리 1GB가 할당되는가?

(A) 그렇다. OS가 즉시 1GB를 물리 메모리에서 확보한다.
(B) 아니다. 가상 주소 공간만 예약되고, 실제 접근 시 물리 메모리가 할당된다.
(C) OS 설정에 따라 다르지만, 일반적으로 즉시 할당된다.
(D) 물리 메모리가 부족하면 즉시 OOM Killer가 발동한다.

---

### Q6. [05장] (2점)

다음 JVM 옵션 중 힙(Heap) 메모리와 **관계없는** 것은?

(A) `-Xms512m`
(B) `-Xmx2g`
(C) `-XX:MaxMetaspaceSize=256m`
(D) `-Xmn256m`

---

### Q7. [06장] (2점)

JVM 힙에서 새로 생성된 객체가 처음 할당되는 영역은?

(A) Old Generation
(B) Survivor 영역
(C) Eden 영역
(D) Metaspace

---

### Q8. [07장] (2점)

GC의 Stop-The-World(STW)가 의미하는 것은?

(A) GC가 완료될 때까지 OS가 다른 모든 프로세스를 중지시킨다
(B) GC가 실행되는 동안 해당 JVM의 모든 애플리케이션 스레드가 일시 정지된다
(C) GC가 실행되면 서버가 재시작된다
(D) GC 중에는 네트워크 요청을 받지 못한다

---

### Q9. [08장] (2점)

**[함정 문제]** Full GC가 실행되어 Old 영역의 객체 500MB를 해제했다. 이때 OS 관점에서 해당 500MB의 물리 메모리는 어떻게 되는가?

(A) OS에 즉시 반환되어 다른 프로세스가 사용할 수 있다
(B) JVM 프로세스가 계속 점유하며, JVM이 나중에 재사용한다
(C) Swap 영역으로 이동된다
(D) 디스크에 기록된 후 해제된다

---

### Q10. [09장] (2점)

서버 모니터링에서 메모리 사용률이 **지속적으로 우상향**하면서 GC 후에도 **기준선이 점점 올라가는** 패턴은 무엇을 의심해야 하는가?

(A) 정상적인 캐시 증가
(B) 메모리 누수(Memory Leak)
(C) 트래픽 증가에 의한 정상 동작
(D) GC 알고리즘이 맞지 않아서

---

## PART 2: 단답형 (8문제 x 3점 = 24점)

> 정확하게 써. "대충 이런 거" 식의 답은 0점이야.

---

### Q11. [01장] (3점)

RAM은 무슨 약자인가? 풀네임을 영어로 쓰고, RAM의 가장 핵심적인 특성 한 가지를 한 문장으로 써라.

---

### Q12. [02장] (3점)

`ps -ef` 출력에서 PPID 컬럼은 무엇을 의미하는가? 한 문장으로 정확하게 써라.

---

### Q13. [03장] (3점)

`free -h` 출력에서 `free` 값이 **0에 가까워도** 메모리 부족이 아닐 수 있다. 이유를 한 문장으로 써라.

---

### Q14. [04장] (3점)

`ps aux` 출력에서 VSZ와 RSS의 차이를 한 문장으로 써라.

---

### Q15. [06장] (3점)

JVM 옵션에서 `-Xms`와 `-Xmx`를 **다르게 설정**하면 (예: `-Xms512m -Xmx2g`) 어떤 현상이 발생하는가? 핵심 한 가지만 써라.

---

### Q16. [07장] (3점)

Minor GC와 Full GC의 대상 영역을 각각 써라.

---

### Q17. [08장] (3점)

"GC가 메모리를 해제한다"와 "OS가 페이지를 회수한다"는 **동작하는 레벨**이 다르다. 각각 어떤 레벨인지 써라.

---

### Q18. [09장] (3점)

서버 메모리 모니터링에서 **"정상"으로 판단하기 위한 핵심 기준 3가지** 중 하나를 써라. (톱니 패턴, GC 후 기준선 등 키워드 사용 가능)

---

## PART 3: 코드/명령어 분석 (7문제 x 5점 = 35점)

> 실전이야. 실제 명령어 출력 읽을 줄 모르면 서버 앞에서 멍때리는 거야.

---

### Q19. [03장] (5점)

아래는 `free -h` 출력이다. 각 질문에 답하라.

```
              total        used        free      shared  buff/cache   available
Mem:           15Gi       9.2Gi       312Mi       128Mi       5.9Gi       5.7Gi
Swap:         4.0Gi       1.2Gi       2.8Gi
```

**(a)** 이 서버의 물리 메모리는 총 얼마인가?
**(b)** `free`가 312Mi밖에 안 된다. 이 서버는 메모리 부족 상태인가? 근거를 들어 답하라.
**(c)** Swap이 1.2Gi 사용 중이다. 이것은 반드시 문제인가? 판단 기준을 써라.

---

### Q20. [02장, 05장] (5점)

아래는 `ps -ef | grep java` 출력의 일부이다. 각 JVM 옵션이 무엇을 의미하는지 번호별로 설명하라.

```
tomcat  12345  1  0 Jan05 ?  03:24:17 /usr/lib/jvm/java-11/bin/java
  -Xms1g          ... (1)
  -Xmx2g          ... (2)
  -XX:+UseG1GC    ... (3)
  -XX:MaxMetaspaceSize=512m  ... (4)
  -Dserver.port=8080         ... (5)
```

**(1)** `-Xms1g`
**(2)** `-Xmx2g`
**(3)** `-XX:+UseG1GC`
**(4)** `-XX:MaxMetaspaceSize=512m`
**(5)** `-Dserver.port=8080`

---

### Q21. [07장] (5점)

아래는 `jstat -gc <PID>` 출력의 일부이다. 각 컬럼이 무엇인지 설명하고, 이 서버의 상태를 판단하라.

```
 S0C    S1C    S0U    S1U      EC       EU        OC         OU       MC     MU
5120.0 5120.0  0.0   3200.0  40960.0  38400.0   102400.0   98200.0  52480.0 49800.0
```

**(a)** `EC`와 `EU`는 각각 무엇인가?
**(b)** `OC`와 `OU`의 관계를 보고 Old 영역 사용률을 계산하라.
**(c)** 이 수치를 보고 이 서버의 상태에 대해 판단하라. (정상/주의/위험)

---

### Q22. [06장] (5점)

아래 다이어그램은 JVM 힙 구조를 나타낸다. 빈칸 (A)~(E)를 채워라.

```
+-----------------------------------------------------+
|                   JVM Heap                            |
|                                                       |
|  +------------------+   +--------------------------+ |
|  | Young Generation |   |    (D) Generation        | |
|  |                  |   |                          | |
|  |  +------+        |   |  오래 살아남은 객체가    | |
|  |  | (A)  | 새 객체|   |  여기로 이동 (Promotion) | |
|  |  +------+        |   |                          | |
|  |  +----+ +----+   |   |  (E) GC 대상 영역       | |
|  |  |(B) | |(C) |   |   |                          | |
|  |  +----+ +----+   |   +--------------------------+ |
|  +------------------+                                 |
+-----------------------------------------------------+
```

**(A)**: ?
**(B)**: ?
**(C)**: ?
**(D)**: ?
**(E)**: ?

---

### Q23. [04장, 09장] (5점)

아래 상황을 읽고 질문에 답하라.

```
서버 상태:
- 물리 메모리: 16GB
- JVM 옵션: -Xms1g -Xmx4g
- 현재 RSS (Resident Set Size): 3.2GB
- 현재 VSZ (Virtual Size): 5.8GB
- 모니터링 대시보드: "메모리 사용률 55%"
```

**(a)** RSS 3.2GB와 VSZ 5.8GB의 차이(2.6GB)는 무엇을 의미하는가?
**(b)** "메모리 사용률 55%"는 무엇을 기준으로 계산된 값일 가능성이 높은가?
**(c)** 이 수치만으로 "메모리 부족"이라고 판단할 수 있는가? 추가로 확인해야 할 것을 하나 써라.

---

### Q24. [10장] (5점)

우리 LMS 서버에서 메모리 사용률이 34%에서 55%로 증가한 사례를 학습했다. 다음 질문에 답하라.

**(a)** 34%에서 55%로 증가한 것이 "문제"라고 단정할 수 없는 이유를 한 가지 써라.
**(b)** 이 상황에서 "정상"인지 "이상"인지 판단하려면 어떤 지표를 확인해야 하는가? 2가지 써라.
**(c)** 만약 GC 후에도 Old 영역 사용률이 계속 올라가면(기준선 상승), 이때 의심해야 할 것은?

---

### Q25. [08장] (5점)

다음 문장의 참/거짓을 판단하고, 거짓인 경우 이유를 써라.

**(a)** "JVM에서 GC가 실행되면, 해제된 메모리는 OS에 즉시 반환된다."
**(b)** "OS는 프로세스가 사용하지 않는 물리 메모리 페이지를 자동으로 회수할 수 있다."
**(c)** "GC가 하는 일과 OS의 페이지 회수가 하는 일은 동일한 레벨의 작업이다."

---

## PART 4: 서술형 (5문제 = 21점)

> 서술형은 **근거**가 전부야. "~인 것 같습니다"는 0점이야.
> 정확한 개념으로, 근거를 들어서 써.

---

### Q26. [01장, 03장] (4점)

"메모리"라는 단어는 문맥에 따라 다른 의미를 가진다. 다음 세 문맥에서 "메모리"가 각각 **구체적으로** 무엇을 가리키는지 설명하라.

- 문맥 1: "이 컴퓨터는 메모리가 16GB야"
- 문맥 2: "이 프로세스가 메모리를 3GB 쓰고 있어"
- 문맥 3: "JVM 힙 메모리를 2GB로 설정했어"

---

### Q27. [03장, 04장] (4점)

**[함정 문제]** 팀원이 "`free -h`에서 free가 거의 0이니까 서버 메모리가 부족하다"라고 보고했다. 이 판단이 **틀릴 수 있는** 이유를 설명하고, **올바른 판단 방법**을 서술하라.

---

### Q28. [06장, 07장] (5점)

`-Xms`와 `-Xmx`를 **같은 값으로 설정하는 것**이 실무에서 권장되는 이유를 다음 관점에서 설명하라.

- (1) 힙 확장 비용 관점
- (2) GC 동작 예측 관점
- (3) 모니터링 관점

---

### Q29. [07장, 08장, 09장] (4점)

아래 모니터링 패턴 2개를 보고, 각각 **정상인지 이상인지** 판단하고 근거를 써라.

```
[패턴 A]
메모리 사용률 그래프:
  80% ┤                              ╱
  70% ┤                         ╱╲╱╱
  60% ┤                    ╱╲╱╱
  50% ┤               ╱╲╱╱
  40% ┤          ╱╲╱╱
  30% ┤     ╱╲╱╱
  20% ┤╱╲╱╱
      └──────────────────────────────── 시간 (24시간)

[패턴 B]
메모리 사용률 그래프:
  70% ┤    ╱╲      ╱╲      ╱╲      ╱╲
  60% ┤  ╱    ╲  ╱    ╲  ╱    ╲  ╱    ╲
  50% ┤╱        ╲        ╲        ╲
  40% ┤          ╱╲        ╱╲       ╱╲
  30% ┤
      └──────────────────────────────── 시간 (24시간)
```

---

### Q30. [전 범위 종합] (4점)

주니어 개발자가 다음과 같이 보고했다:

> "서버 메모리 사용률이 55%입니다. 16GB 중 약 9GB를 사용 중이고, free -h에서 free는 300MB이고, Swap을 1GB 쓰고 있습니다. 문제 있는 것 같습니다."

이 보고에 대해 **빠싺 시니어**로서 어떤 추가 질문을 하고, 어떻게 판단할 것인지 서술하라. 최소 3가지 관점에서 답하라.

---

!!! danger "여기까지."

    *"Not quite my tempo?"* → 처음부터 다시 읽어.

    *"Were you rushing or were you dragging?"* → 둘 다 안 돼. **정확하게, 근거 있게.**
