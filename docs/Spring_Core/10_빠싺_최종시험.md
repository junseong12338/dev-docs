# 10. 빠싺 최종시험

**규칙**: 답을 안 보고 풀어야 한다. 답 못 하면 해당 챕터 다시 읽기.

---

## Part 1: Spring 기본 (01장)

### Q1. Spring이 뭔지 한 문장으로 설명해봐.

```
힌트: "~를 ~하는 ~"
```

---

### Q2. IoC(Inversion of Control)가 뭐야?

```
"제어"가 뭐고, "역전"이 뭔지 각각 설명해봐.
```

---

### Q3. 라이브러리와 프레임워크의 차이는?

```
"누가 누구를 호출하느냐"로 설명해봐.
```

---

## Part 2: Bean (02장)

### Q4. Bean이 뭐야? 일반 객체와 뭐가 달라?

```
3가지 차이점을 말해봐.
```

---

### Q5. 아래 XML을 Java 코드로 바꿔봐.

```xml
<bean id="userService" class="com.example.UserServiceImpl">
    <property name="name" value="hello" />
    <property name="repository" ref="userRepo" />
</bean>
```

```
Spring이 내부적으로 하는 일을 Java 코드로 써봐.
```

---

### Q6. value와 ref의 차이는?

```
각각 언제 쓰는지 말해봐.
```

---

## Part 3: DI (03장)

### Q7. DI(Dependency Injection)가 뭐야?

```
"의존성"이 뭐고, "주입"이 뭔지 각각 설명해봐.
```

---

### Q8. DI 주입 방식 3가지를 말하고 각각의 장단점을 설명해봐.

---

### Q9. @Resource와 @Autowired의 차이는?

```
검색 방식의 차이로 설명해봐.
```

---

### Q10. 왜 직접 new 안 하고 DI를 쓰는 거야?

```
new로 직접 만들 때의 문제점 3가지를 말해봐.
```

---

## Part 4: XML 설정 (04장)

### Q11. 아래 XML의 각 줄을 해석해봐.

```xml
<bean id="sqlSession" class="org.mybatis.spring.SqlSessionFactoryBean">
    <property name="dataSource" ref="dataSource" />
    <property name="mapperLocations"
              value="classpath*:/egovframework/sqlmap/${framework.database.db_type}/*/*.xml" />
</bean>
```

```
한 줄씩 뭘 하는 건지 말해봐.
```

---

### Q12. classpath:와 classpath*:의 차이는?

---

### Q13. ${framework.database.db_type}이 뭐야? 어디서 오는 값이야?

---

## Part 5: Bean 생명주기 (05장)

### Q14. Bean 생명주기 5단계를 순서대로 말해봐.

---

### Q15. afterPropertiesSet()은 언제 호출돼? 왜 중요해?

```
우리 프로젝트 RefreshableSqlSessionFactoryBean에서
이 메서드가 하는 일을 설명해봐.
```

---

### Q16. destroy()는 언제 호출돼? 안 하면 뭐가 문제야?

---

## Part 6: Bean Scope (06장)

### Q17. singleton과 prototype의 차이를 설명해봐.

---

### Q18. Spring Bean의 기본 Scope는? 왜 그게 기본이야?

```
이유를 3가지 말해봐.
```

---

### Q19. singleton Bean에 상태(필드 값)를 저장하면 왜 위험해?

```
구체적인 시나리오로 설명해봐.
```

---

## Part 7: 어노테이션 (07장)

### Q20. @Component, @Service, @Repository, @Controller의 관계와 차이는?

---

### Q21. component-scan이 뭐야? 왜 필요해?

---

### Q22. XML로 Bean 등록하는 것과 어노테이션으로 등록하는 것, 각각 언제 쓰냐?

```
판단 기준을 말해봐.
```

---

## Part 8: Spring Context (08장)

### Q23. Root ApplicationContext와 Servlet ApplicationContext의 차이는?

```
각각 뭘 담당하고, 누가 생성하는지 말해봐.
```

---

### Q24. 자식 Context에서 부모 Context의 Bean을 쓸 수 있어? 반대는?

---

### Q25. component-scan 범위가 Root와 Servlet에서 겹치면 뭐가 문제야?

---

## Part 9: 우리 프로젝트 실전 (09장)

### Q26. 우리 프로젝트에서 요청 1개가 처리되는 Bean 흐름을 말해봐.

```
Browser → ??? → ??? → ??? → DB
각 단계에서 어떤 Bean이 관여하는지.
```

---

### Q27. context-sqlMap.xml에서 class를 변경한 이유를 설명해봐.

```
RefreshableSqlSessionFactoryBean → SqlSessionFactoryBean
왜 바꿨는지, 바꾸면 뭐가 달라지는지.
```

---

### Q28. interval=1000이 메모리 누수를 일으킨 과정을 Bean 생명주기 관점에서 설명해봐.

```
"3단계(초기화)에서 ~~~가 시작되고..."로 시작해봐.
```

---

### Q29. ref="dataSource"에서 dataSource Bean은 어디에 정의되어 있어?

```
파일 간 참조 관계를 설명해봐.
```

---

### Q30. Spring XML 설정 한 줄의 무게를 설명해봐.

```
class 속성 하나, property 하나가 서버 전체에 미치는 영향을
우리 프로젝트 사례로 설명해봐.
```

---

## 채점 기준

| 등급 | 맞은 수 | 수준 |
|------|---------|------|
| F | 0~10개 | 처음부터 다시 |
| D | 11~15개 | 기초 부족, 01~03장 다시 읽기 |
| C | 16~20개 | 개념은 있는데 빈틈 있음 |
| B | 21~25개 | 괜찮은데 실전 감각 부족 |
| A | 26~28개 | 충분히 이해함 |
| S | 29~30개 | 빠싺 인정 |

---

## 답안

답은 각 챕터에 다 있다. 답 모르겠으면 해당 챕터 번호 보고 다시 읽어.

```
Q1~Q3:   01장
Q4~Q6:   02장
Q7~Q10:  03장
Q11~Q13: 04장
Q14~Q16: 05장
Q17~Q19: 06장
Q20~Q22: 07장
Q23~Q25: 08장
Q26~Q30: 09장
```

답 보고 외우는 건 Lv1이야.
**왜 그런지**까지 설명할 수 있어야 Lv3.
**다르게 할 수 있는 방법**까지 말할 수 있어야 Lv4.
**자다가 일어나서도 설명 가능**하면 Lv5.
