# 09. 빠싺 최종시험 - Black Hole

---

!!! danger ""
    **"There are no two words in the English language more harmful than 'good job'."**

    01~08 전 범위. 30문제. 100점 만점.
    여기서 80점 못 넘기면 처음부터 다시 읽어.

## 시험 규칙

| 항목 | 규칙 |
|------|------|
| **합격 기준** | 80점 이상 |
| **시간 제한** | 60분 |
| **오픈북** | 금지. 머리에 없으면 모르는 거야. |
| **검색** | 금지. 구글 켜는 순간 불합격. |
| **부분 점수** | 서술형만 부분 점수 있음 |
| **총점** | 100점 |

---

## 객관식 (10문제 × 2점 = 20점)

### Q1. [Ch01] (2점)

API 호출에서 "호출 코드"를 짜는 건 누구야?

- (A) 받는 쪽
- (B) 보내는 쪽
- (C) 양쪽 다
- (D) 아무도 안 짜도 됨

---

### Q2. [Ch02] (2점)

Webhook에서 "보내는 코드"를 짜는 건 누구야?

- (A) 우리 (NC 개발자)
- (B) 플랫폼 (Zoom 개발자)
- (C) 양쪽 다
- (D) LMS 개발자

---

### Q3. [Ch03] (2점)

NC가 LMS한테 출결 데이터를 전송하는 것은?

- (A) Pull이면서 Webhook
- (B) Push이면서 Webhook
- (C) Pull이면서 API 호출
- (D) Push이면서 API 호출

---

### Q4. [Ch04] (2점)

Webhook의 3단계 중 "한 번만" 하는 것은?

- (A) 이벤트 감지
- (B) HTTP 요청 전송
- (C) URL 등록
- (D) 서명 검증

---

### Q5. [Ch05] (2점)

Webhook과 API 호출의 기술적 차이는?

- (A) Webhook은 TCP, API 호출은 HTTP
- (B) Webhook은 JSON, API 호출은 XML
- (C) 기술적 차이 없음. 둘 다 HTTP 요청
- (D) Webhook은 비동기, API 호출은 동기

---

### Q6. [Ch05] (2점)

Webhook인지 API 호출인지 판별하는 핵심 질문은?

- (A) "데이터를 보내냐 받냐?"
- (B) "보내는 쪽에 URL 등록하는 설정 페이지가 있냐?"
- (C) "HTTP Method가 POST냐 GET이냐?"
- (D) "서버 대 서버 통신이냐?"

---

### Q7. [Ch06] (2점)

학생이 Zoom 강의실에 들어갔다 나가면 발생하는 이벤트 순서는?

- (A) meeting.ended → participant_joined
- (B) participant_joined → meeting.ended
- (C) participant_joined → participant_left
- (D) participant_left → participant_joined

---

### Q8. [Ch07] (2점)

Webhook 서명 검증에 사용하는 알고리즘은?

- (A) AES-256
- (B) RSA-2048
- (C) HMAC-SHA256
- (D) MD5

---

### Q9. [Ch07] (2점)

Zoom이 Webhook을 보냈는데 NC가 응답하지 않으면?

- (A) Zoom이 포기하고 데이터 삭제
- (B) Zoom이 자동으로 재시도 (최대 3회)
- (C) NC가 나중에 Zoom한테 달라고 요청
- (D) LMS가 대신 받아줌

---

### Q10. [Ch08] (2점)

WebhookController에서 `@RequestBody`를 `String`으로 받는 이유는?

- (A) JSON 파싱이 느려서
- (B) 서명 검증에 HTTP 요청 원문이 필요해서
- (C) Zoom이 String으로 보내서
- (D) Spring Boot가 Map을 지원 안 해서

---

## 단답형 (8문제 × 3점 = 24점)

### Q11. [Ch01] (3점)

API 호출의 3요소는?

---

### Q12. [Ch02] (3점)

"Webhook"이라는 단어에서 "Hook"의 의미는?

---

### Q13. [Ch03] (3점)

Push와 Pull의 차이를 한 문장으로 설명해.

---

### Q14. [Ch04] (3점)

Zoom Marketplace에서 Webhook 설정할 때 입력하는 3가지는?

---

### Q15. [Ch05] (3점)

소셜 로그인(네이버 로그인)이 Webhook이 아닌 이유를 한 문장으로.

---

### Q16. [Ch06] (3점)

NexClass에서 사용하는 Zoom Webhook 이벤트 3가지를 쓰시오.

---

### Q17. [Ch07] (3점)

멱등성(Idempotency)이란?

---

### Q18. [Ch08] (3점)

JOIN_LOG 테이블과 ATTENDANCE 테이블을 분리하는 이유는?

---

## 코드 작성/분석 (7문제 × 5점 = 35점)

### Q19. [Ch04] (5점)

Spring Boot에서 Zoom Webhook을 받는 Controller 메서드의 **시그니처(메서드 선언부)**를 작성해. (어노테이션 포함)

---

### Q20. [Ch07] (5점)

다음 코드의 빈칸을 채워 서명 검증 로직을 완성해.

```java
public boolean verifySignature(String body, String timestamp, String signature) {
    String message = "v0:" + _______ + ":" + _______;

    Mac mac = Mac.getInstance("HmacSHA256");
    mac.init(new SecretKeySpec(
        _______.getBytes(), "HmacSHA256"
    ));
    String computed = "v0=" + Hex.encodeHexString(
        mac.doFinal(message.getBytes(StandardCharsets.UTF_8))
    );

    return computed.equals(_______);
}
```

---

### Q21. [Ch06] (5점)

학생이 09:05에 입장, 09:25에 퇴장, 09:30에 재입장, 09:50에 퇴장했다.

아래 빈칸을 채워라.

| 세션 | 입장 | 퇴장 | 참여시간 |
|------|------|------|---------|
| 1 | 09:05 | _____ | _____분 |
| 2 | _____ | 09:50 | _____분 |
| **합계** | | | _____분 |

출결 판정 기준이 30분일 때, 이 학생은 출석(P)인가 결석(A)인가?

---

### Q22. [Ch08] (5점)

meeting.ended 이벤트가 왔을 때 처리하는 코드의 흐름을 **순서대로** 4단계로 쓰시오.

---

### Q23. [Ch07] (5점)

다음 상황에서 발생하는 문제와 해결 방법을 쓰시오.

```
Zoom이 participant_joined 이벤트를 보냈으나 NC가 응답 못 함
→ Zoom이 5분 후 동일 이벤트를 재전송
→ NC가 두 번째 요청을 정상 처리
→ 문제: ???
→ 해결: ???
```

---

### Q24. [Ch04, Ch05] (5점)

다음 코드가 Webhook인지 API 호출인지 판별하고, 그 이유를 설명해.

```java
// 코드 A
@PostMapping("/webhook/zoom")
public ResponseEntity<String> receive(@RequestBody String body) {
    processEvent(body);
    return ResponseEntity.ok("OK");
}

// 코드 B
public void sendAttendance(String lessonCd) {
    http.post("https://lms-server/api/attendance", attendanceData);
}
```

---

### Q25. [Ch02, Ch05] (5점)

"Webhook은 API 호출의 한 종류다."라는 문장이 맞는 이유를 3줄 이내로 설명해.

---

## 서술형 (5문제 = 21점)

### Q26. [Ch02, Ch05] (4점)

Webhook과 일반 API 호출의 차이를 **URL 등록 시스템** 관점에서 설명해. 예시를 하나 포함할 것.

---

### Q27. [Ch03, Ch05] (4점)

"Push이면 무조건 Webhook이다"라는 주장이 **틀린 이유**를 NexClass 프로젝트 예시를 들어 설명해.

---

### Q28. [Ch06, Ch08] (5점)

NexClass에서 Zoom Webhook을 받아서 LMS에 출결을 전달하는 **전체 흐름**을 설명해. 다음 키워드를 반드시 포함할 것: Zoom Marketplace, participant_joined, meeting.ended, JOIN_LOG, ATTENDANCE, API 호출

---

### Q29. [Ch07] (4점)

Webhook 보안에서 서명 검증(HMAC-SHA256)이 필요한 이유를 설명하고, 서명 검증이 없으면 어떤 공격이 가능한지 구체적으로 쓰시오.

---

### Q30. [Ch02, Ch05] (4점)

주니어 개발자가 "NC → LMS 출결 전송도 Webhook 아니에요? 이벤트 발생하면 자동으로 보내는 건데요?"라고 물었다. 이 개발자에게 **왜 Webhook이 아닌지** 설명해줘.

---

**시험 끝. 정답은 10번 파일에서 확인해.**

**"Not quite my tempo? 다시."**
